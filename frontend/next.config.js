/** @type {import('next').NextConfig} */
  const nextConfig = {
    experimental: {
      serverActions: true,
    },
    // Add any specific configurations needed
  };

  module.exports = nextConfig;